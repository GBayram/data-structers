#include <stdio.h>

/*int main() {

	int *p; // Pointer tanimlanmasi.
	int a = 10;
	p = &a; // a degiskeninin adres bilgisini p pointer'ine ata.
	printf("%d\n", *p); // Ciktimiz: 10 olacaktir. *p => gosterdigi adresin icindeki degeri ekrana yazdir.
	printf("%u\n", p); // p'nin icerisinde tutulan adresi ekrana basacak.
	printf("%u", &a); // a'nin adresini ekrana yazacak. Yani bir ustteki bastirdigimiz degerle a'nin adresi ayni cikacak.

}*/